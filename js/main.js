
//Creando las variables

var lista_formulario = getElementById('lista');
var boton = document.createElement('boton');
var texto = document.createTextNodo("Guardar")
lista_formulario.appenChild(boton);
boton.appenChild(texto);


lista_formulario.addEventListener('click', function()){

	lista_formulario.addEventListener('click',function(){
         

	})

}